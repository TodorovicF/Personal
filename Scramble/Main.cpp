#include "Scramble.h"

int main() {
	Scramble testScramble;
	testScramble.start();

	/*BinaryTree<int> newTree;
	for (int i = 0; i < 50; i++) {
		newTree.insertData(i);
		cout << newTree.containsData(i);
	}*/
};
