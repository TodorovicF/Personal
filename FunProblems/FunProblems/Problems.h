#include <algorithm>
#include <string>
#include <iostream>

using namespace std;

class Problems {
public:
	int smallestDivisibleInRange(int from, int to);
	string reverseString(string toReverse);
};
